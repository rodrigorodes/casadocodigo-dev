spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:mercadolivredb
    sql-script-encoding: UTF-8
    username: sa
    password: 
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
    open-in-view: false